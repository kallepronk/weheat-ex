Fixtures are synthetic, generated by `tools/gen_dto.py` in the parent workspace from the
Python SDK's field declarations. Replace with scrubbed recordings of real responses once
credentials are available.
